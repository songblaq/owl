---
id: plan-2026
status: active
created: 2026-04-11
---

# oh-my-brain — 전체 계획 (2026)

개인 LLM-maintained 지식 시스템 **oh-my-brain(omb)**의 목표, 아키텍처, 로드맵을 한 문서에 기록한다.

---

## 1. 프로젝트 목적

> **"RAG가 아니라 잘 유지된 wiki가 답"** — Karpathy 2026

LLM 세션이 끝나도 지식이 쌓이는 시스템. 3가지 전략(owl / cairn / wiki)으로 동일 source를 운영하고 benchmark로 승자를 선택한다. 승자는 `omb` CLI에 네이티브로 통합된다.

---

## 2. 시스템 아키텍처

```mermaid
graph TB
    subgraph Sources["📦 Source Layer (불변)"]
        RAW["~/owl-vault/raw/\n125 files\n(절대 편집 금지)"]
    end

    subgraph Views["🔭 View Layer"]
        OWL["owl view\n~/owl-vault\n232 compiled\nKarpathy LLM Wiki"]
        CAIRN["cairn view\n~/cairn-vault\n421 entries\natomic claims"]
        WIKI["wiki view\n~/brain-vault/wiki/\n(Phase V — 미구현)"]
    end

    subgraph Hub["🔗 brain-vault symlink hub"]
        BV["~/brain-vault/\n  sources/ → raw\n  owl/ → owl-vault\n  cairn/ → cairn-vault\n  wiki/"]
    end

    subgraph CLI["⌨️ CLI Layer"]
        OMB["omb (통합)\nv0.1.0"]
        OWLCLI["owl\nv0.1.0\n9 subcommands"]
        CAIRNCLI["cairn\nv0.0.1\n5 subcommands"]
    end

    subgraph ClaudeCode["🤖 Claude Code 연동"]
        CMDS["slash commands\n/owl-search\n/owl-ingest\n/owl-compile\n/owl-promote\n/owl-file\n/owl-health\n/owl-query"]
        AGENTS["subagents\nowl-compiler\nowl-librarian\nowl-health"]
        HOOKS["vault hooks\nsession_start × 2\npre_tool × 2\npost_tool × 1"]
    end

    RAW -->|"ingest"| OWL
    RAW -->|"ingest"| CAIRN
    RAW -->|"ingest (Phase V)"| WIKI
    OWL & CAIRN & WIKI --> BV
    OMB -->|"subprocess"| OWLCLI
    OMB -->|"subprocess"| CAIRNCLI
    OWLCLI -->|"read/write"| OWL
    CAIRNCLI -->|"read/write"| CAIRN
    OWLCLI --> CMDS & AGENTS & HOOKS
    CAIRNCLI -.->|"Phase T"| CMDS & AGENTS & HOOKS
```

---

## 3. 지식 생애주기

```mermaid
flowchart LR
    IN["📥 inbox\n(raw 자료 투입)"]

    subgraph OWL_FLOW["owl 흐름"]
        direction TB
        O1["owl ingest\nraw/ 에 저장"]
        O2["owl compile\nLLM → summary/note"]
        O3["owl promote\nconcept/index 승격"]
        O1 --> O2 --> O3
    end

    subgraph CAIRN_FLOW["cairn 흐름"]
        direction TB
        C1["cairn sources/\n에 drop"]
        C2["cairn-scribe\natomic claim 생성\n(Phase T)"]
        C3["cairn index\nINDEX.md 재생성"]
        C1 --> C2 --> C3
    end

    SEARCH["🔍 omb search\n통합 검색"]
    USE["💬 Claude 세션\n지식 활용"]
    BACK["🔄 세션 결과\n→ inbox 환류"]

    IN --> OWL_FLOW & CAIRN_FLOW
    OWL_FLOW --> SEARCH
    CAIRN_FLOW --> SEARCH
    SEARCH --> USE
    USE --> BACK
    BACK --> IN
```

---

## 4. 현재 상태 (2026-04-11)

| 항목 | 상태 |
|---|---|
| 리포 | `songblaq/oh-my-brain` · `~/_/projects/oh-my-brain` |
| Phase A–S | 완료 — owl 단독 18 phases + monorepo restructure |
| Phase omb | 완료 — unified CLI, GitHub rename, pipx reinstall |
| owl | v0.1.0 · raw 125 · compiled 233 · health 195 issues |
| cairn | v0.0.1 · entries 421 · Claude Code 연동 없음 |
| wiki | spec only (미구현) |
| omb | v0.1.0 · owl+cairn delegator · wiki placeholder |

---

## 5. 로드맵

```mermaid
gantt
    title oh-my-brain 로드맵 2026
    dateFormat YYYY-MM
    axisFormat %b

    section 완료
    A–S (owl + monorepo)     :done, a, 2026-04, 2026-04
    omb CLI                   :done, b, 2026-04, 2026-04

    section Phase T
    cairn Claude Code 연동   :active, t, 2026-04, 2026-05

    section Phase U
    테스트 + CI              :u, after t, 1M

    section Phase V
    wiki view + benchmark-v2 :v, after u, 1M

    section Phase W
    동기화 + omb 수렴        :w, after v, 2M
```

---

## 6. Phase T — cairn Claude Code 연동

**목표:** cairn을 owl 수준의 Claude Code 시민으로 만든다.

```mermaid
flowchart TD
    T["Phase T 시작"]

    T --> T1["slash commands 작성\n/cairn-search\n/cairn-ingest"]
    T --> T2["subagents 작성\ncairn-scribe\ncairn-auditor"]
    T --> T3["vault hook 설치\nsession_start:\n  → INDEX.md 주입\npre_tool:\n  → lockfile 획득"]

    T1 & T2 & T3 --> T4["symlink 등록\n~/.claude/commands/\n~/.claude/agents/"]
    T4 --> T5["smoke test\n/cairn-search 동작\ncairn-scribe entry 생성"]
    T5 --> T6["CLAUDE.md 업데이트\ncairn 연동 섹션"]
    T6 --> DONE_T["✅ Phase T 완료"]
```

### T 세부 태스크

| # | 작업 | 산출물 |
|---|---|---|
| T-1 | `/cairn-search` 슬래시 명령 | `views/cairn/src/cairn/claude_assets/commands/cairn-search.md` |
| T-2 | `/cairn-ingest` 슬래시 명령 | `views/cairn/src/cairn/claude_assets/commands/cairn-ingest.md` |
| T-3 | `cairn-scribe` 서브에이전트 | `views/cairn/src/cairn/claude_assets/agents/cairn-scribe.md` |
| T-4 | `cairn-auditor` 서브에이전트 | `views/cairn/src/cairn/claude_assets/agents/cairn-auditor.md` |
| T-5 | session_start hook (INDEX.md 주입) | `~/cairn-vault/.claude/settings.json` |
| T-6 | `cairn setup-claude` CLI 서브커맨드 | symlink 자동 등록 |
| T-7 | CLAUDE.md cairn 섹션 보강 | `~/.claude/CLAUDE.md` |

---

## 7. Phase U — 테스트 + CI

**목표:** 코드베이스에 pytest 기반 자동화 테스트와 GitHub Actions CI를 붙인다.

```mermaid
flowchart TD
    U["Phase U 시작"]
    U --> U1["owl 테스트 작성\nsearch / health / vault / ingest"]
    U --> U2["cairn 테스트 작성\nentry / search / index / lockfile"]
    U --> U3["omb 통합 테스트\nomb status / omb search"]
    U1 & U2 & U3 --> U4["GitHub Actions 워크플로우\npush → pytest + ruff"]
    U4 --> DONE_U["✅ Phase U 완료"]
```

### U 세부 태스크

| # | 작업 |
|---|---|
| U-1 | `views/owl/tests/` pytest 초기화 |
| U-2 | owl search / health / vault roundtrip 테스트 |
| U-3 | `views/cairn/tests/` pytest 초기화 |
| U-4 | cairn entry CRUD + index 재생성 테스트 |
| U-5 | `views/omb/tests/` 통합 테스트 |
| U-6 | `.github/workflows/ci.yml` (Python 3.14, pytest, ruff) |

---

## 8. Phase V — wiki view + benchmark-v2

**목표:** wiki(pure Karpathy control group)를 구현하고 3-way 실전 benchmark를 수행한다.

```mermaid
flowchart TD
    V["Phase V 시작"]
    V --> V1["wiki CLI 작성\nwiki ingest / search / status"]
    V --> V2["~/brain-vault/wiki/\n에 source ingest"]
    V1 & V2 --> V3["benchmark-v2 설계\n10개 실제 질문\n3 view 비교"]
    V3 --> V4["benchmark 실행\nowl vs cairn vs wiki\n각 view 응답 품질 평가"]
    V4 --> V5["결과 기록\ndocs/benchmark-v2.md"]
    V5 --> V6{"승자 선택"}
    V6 -->|"owl 승리"| V7A["omb native = owl 기반"]
    V6 -->|"cairn 승리"| V7B["omb native = cairn 기반"]
    V6 -->|"wiki 승리"| V7C["omb native = wiki 기반"]
    V6 -->|"혼합"| V7D["hybrid: 목적별 routing"]
    V7A & V7B & V7C & V7D --> DONE_V["✅ Phase V 완료"]
```

---

## 9. Phase W — 동기화 + omb 수렴

**목표:** owl → cairn 자동 sync, source layer 독립, 최종적으로 omb에 승자 view를 내재화한다.

```mermaid
flowchart TD
    W["Phase W 시작"]
    W --> W1["owl → cairn freshness loop\nowl compile 결과 → cairn entry 자동 생성"]
    W --> W2["source layer 분리\nbrain-source-lanes 통합\nsources/ 독립 repo 검토"]
    W --> W3["cairn tag taxonomy\n현행 6 type 재검토 + 정리"]
    W1 & W2 & W3 --> W4["omb 내재화\n승자 view 로직을 omb에 직접 이식\n(subprocess 대신 native call)"]
    W4 --> W5["legacy CLI 정책 결정\nowl / cairn 독립 유지 vs omb 흡수"]
    W5 --> DONE_W["✅ Phase W 완료 — omb v1.0"]
```

---

## 10. 핵심 설계 원칙

| 원칙 | 내용 |
|---|---|
| raw 불변 | `raw/` 파일은 읽기 전용. 수정 금지 |
| vault = 데이터만 | 코드는 repo에, 파생 데이터는 vault에 |
| view 독립 | 각 view는 서로의 산출물을 input으로 쓰지 않음 |
| rebuildable | source만 있으면 모든 view 재생성 가능 |
| CLI = decidable | CLI는 JSON 출력, LLM이 해석 |
| evidence 동반 | 기록 시 결과 + 근거(왜, 대안 비교, 검증) 함께 |
| omb wraps, doesn't replace | 점진 통합. 기존 CLI 호환 유지 |

---

## 11. 파일 지도

```
~/_/projects/oh-my-brain/
├── docs/
│   ├── plan-2026.md          ← 이 파일 (전체 계획)
│   ├── view-contract.md      view 인터페이스 계약
│   ├── source-layer-spec.md  source 불변 계약
│   ├── benchmark-v0.md       마이그레이션 benchmark
│   └── benchmark-v1.md       owl vs cairn full benchmark
├── views/
│   ├── owl/                  owl view (v0.1.0)
│   ├── cairn/                cairn view (v0.0.1)
│   ├── omb/                  unified CLI (v0.1.0)
│   └── wiki/                 spec only → Phase V
└── .github/
    └── workflows/ci.yml      → Phase U
```
